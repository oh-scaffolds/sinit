'use strict';

var assert = require('assert');
var yaml   = require('../../index');


suite('unit', function () {
  suite('unit one', function () {
    test('test case one', function () {

    });
});
